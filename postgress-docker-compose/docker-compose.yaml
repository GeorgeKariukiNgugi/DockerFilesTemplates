version: '3.9'
services:
  postgres:
    image: postgres:latest
    ports: 
      - 5433:5432
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root     
    volumes:
      - ./data:/var/lib/postgresql/data

